# React + TypeScript frontend for the utility ticket map application.

## What it does

- Displays utility tickets on a map
- Sends bbox map bounds to the backend
- Shows filtered tickets returned from the API
- Uses React, TypeScript, Vite, and Leaflet

## Run

```bash
npm install
npm run dev
```
